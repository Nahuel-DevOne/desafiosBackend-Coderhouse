-- Configuración para MYSQL (Todas las sentencias terminan con ;)

-- Inicializador: mysql -h 127.0.0.1 -P 3306 -u root -p

-- Mostrar DB: show databases;

-- Crear DB: create database nombre;

-- Eliminar DB: drop database nombre;

-- Usar/Acceder DB: use nombre;

-- Ver la tabla: show full tables;

-- Leer tablas (Con * selecciona todo): select * from nombre de la tabla;

-- Crear registros: insert into nombre de tabla (Campos no incrementales de la tabla) values (Valores de cada campo separados por ,)

-- Actualizar registros: update nombre de tabla set campo = nuevo valor where nombre de campo = valor de la tabla;

-- Borrar contenido de una tabla = truncate nombre de la tabla







