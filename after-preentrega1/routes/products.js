const express = require('express');
const fs = require('fs');
const {Router} = express

const router = new router();

// router.get('/', (req, res) => { 
//     if(req.params.id) {
//         fs.readFileSync("../products.")
// });

router.post('/', (req, res) => { });
router.post('/', (req, res) => { });
router.delete('/', (req, res) => { });
router.delete('/', (req, res) => { });
router.put('/', (req, res) => { });
