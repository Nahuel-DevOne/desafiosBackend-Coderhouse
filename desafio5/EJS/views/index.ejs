<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>All Products</title>
  </head>
  <body>
    <h1 class="display-2 text-dark text-center bg-light p-3 mb-5">Todos nuestros productos</h1>
    <div class="container">
        <div class="row justify-content-center">
            <div class="cards d-flex flex-wrap g-3">
                <% products.forEach(function(product){ %>
              <div class="card m-3 mt-4 bg-light align-items-center justify-content-center p-2 m-auto">
                <img src="<%= product.thumbnail %>" alt="<%= product.title %>" />
                <h3 class="card-title"><%= product.title %></h3>
                <p class="card-title">$<%= product.price %></p>
              </div>
              <% }); %>
            </div>
            <a href="/" class="btn btn-lg btn-danger text-white lead fs-3 col-12 col-sm-9 col-md-6 col-lg-3 m-3 mb-5">Agrega un producto</a>
        </div>
    </div>
  </body>
</html>